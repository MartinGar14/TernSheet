# Google Sheet to write application data into (the long ID in the sheet's URL)
SHEET_ID=

# Optional: name of the tab/worksheet within the sheet (defaults to first sheet)
SHEET_NAME=

# LLM API key for classification/extraction (added in a later step)
# ANTHROPIC_API_KEY=
